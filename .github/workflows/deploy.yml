name: Deploy to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:  # 수동 실행 가능

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 15m
          script: |
            # 필수 패키지 설치 (python3-venv)
            sudo apt-get update -qq
            sudo apt-get install -y -qq python3-venv git curl || true

            # 배포 디렉토리 생성
            mkdir -p ~/rag-app
            cd ~/rag-app

            # 임시 디렉토리에서 전체 저장소 클론
            TEMP_DIR=$(mktemp -d)
            cd $TEMP_DIR
            git clone --depth 1 https://github.com/${{ github.repository }}.git temp-repo

            # app 폴더와 requirements.txt만 ~/rag-app으로 복사
            if [ -d "temp-repo/app" ]; then
              cp -r temp-repo/app/* ~/rag-app/
            else
              echo "❌ app 폴더를 찾을 수 없습니다."
              exit 1
            fi
            cp temp-repo/requirements.txt ~/rag-app/ 2>/dev/null || echo "requirements.txt가 루트에 없습니다."

            # 정리
            cd ~
            rm -rf $TEMP_DIR

            # main.py 수정: app.api_server -> api_server (루트 디렉토리 기준)
            if [ -f "main.py" ]; then
              # sed를 사용하여 안전하게 수정
              sed -i 's/"app\.api_server:app"/"api_server:app"/g' main.py || true
              sed -i "s/'app\.api_server:app'/'api_server:app'/g" main.py || true
              echo "✅ main.py 수정 완료"
            fi

            # 가상환경 생성/업데이트
            if [ ! -d "venv" ]; then
              python3 -m venv venv || {
                echo "⚠️ venv 생성 실패, python3-venv 재설치 시도"
                sudo apt-get install -y python3-venv
                python3 -m venv venv
              }
            fi
            source venv/bin/activate || {
              echo "❌ venv 활성화 실패"
              exit 1
            }
            pip install --upgrade pip
            pip install -r requirements.txt

            # 환경 변수 업데이트
            LLM_PROVIDER_VALUE="${{ secrets.LLM_PROVIDER }}"
            if [ -z "$LLM_PROVIDER_VALUE" ]; then
              LLM_PROVIDER_VALUE="openai"
            fi
            echo "POSTGRES_CONNECTION_STRING=${{ secrets.POSTGRES_CONNECTION_STRING }}" > .env
            echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env
            echo "LLM_PROVIDER=${LLM_PROVIDER_VALUE}" >> .env
            echo "HOST=0.0.0.0" >> .env
            echo "PORT=8000" >> .env
            echo "RELOAD=false" >> .env
            # .env 파일 권한 설정 (보안)
            chmod 600 .env

            # Systemd 서비스 재시작 (없으면 경고만)
            if systemctl list-units --type=service --all | grep -q rag-api.service; then
              sudo systemctl restart rag-api
              sleep 5
              if curl -f http://localhost:8000/health; then
                echo "✅ 배포 성공! 서비스가 정상 작동 중입니다."
              else
                echo "⚠️ 헬스 체크 실패. 서비스 상태를 확인하세요."
                sudo journalctl -u rag-api -n 50 || true
                exit 1
              fi
            else
              echo "⚠️ Systemd 서비스가 설정되지 않았습니다."
              echo "   파일 배포는 완료되었습니다."
              echo "   서비스를 설정하려면: scripts/setup_systemd.sh 실행"
              echo "✅ 파일 배포 완료 (서비스 설정 필요)"
            fi

      - name: Notify deployment status
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "🎉 배포 성공!"
          else
            echo "⚠️ 배포 실패! 로그를 확인하세요."
          fi

