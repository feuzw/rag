# PostgreSQL 연결 설정
POSTGRES_CONNECTION_STRING=postgresql://user:password@host:port/dbname

# OpenAI API 키
OPENAI_API_KEY=sk-your-openai-api-key-here

# LLM 제공자 (openai 또는 midm)
LLM_PROVIDER=openai

# 서버 설정
HOST=0.0.0.0
PORT=8000

# 로컬 모델 경로 (Mi:dm 사용 시)
# LOCAL_MODEL_DIR=/path/to/model

